# Netflix Clone (Vite + React + TypeScript)

Quick start:

```powershell
cd "c:\Users\akash\OneDrive\Desktop\netflix-clone"
npm install
npm run dev
```

Files added:
- `package.json` — scripts and deps
- `vite.config.ts` — Vite config with React plugin
- `index.html`, `src/main.tsx`, `src/App.tsx`, `src/index.css`
- `tsconfig.json`

Want me to run `npm install` and start the dev server now? Reply yes to proceed.
